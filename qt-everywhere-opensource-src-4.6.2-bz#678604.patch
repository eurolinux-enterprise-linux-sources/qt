diff -up qt-everywhere-opensource-src-4.6.2/src/gui/kernel/qwidget_x11.cpp.me qt-everywhere-opensource-src-4.6.2/src/gui/kernel/qwidget_x11.cpp
--- qt-everywhere-opensource-src-4.6.2/src/gui/kernel/qwidget_x11.cpp.me	2012-07-06 17:31:01.000000000 +0200
+++ qt-everywhere-opensource-src-4.6.2/src/gui/kernel/qwidget_x11.cpp	2012-07-06 17:32:36.000000000 +0200
@@ -903,8 +903,10 @@ void QWidgetPrivate::create_sys(WId wind
                 q->setWindowOpacity(maybeTopData()->opacity/255.);
 
         }
-    } else if (q->testAttribute(Qt::WA_SetCursor) && q->internalWinId()) {
+    } else if (q->internalWinId()) {
         qt_x11_enforce_cursor(q);
+        if (QWidget *p = q->parentWidget()) // reset the cursor on the native parent
+            qt_x11_enforce_cursor(p);
     }
 
     if (extra && !extra->mask.isEmpty() && q->internalWinId())
